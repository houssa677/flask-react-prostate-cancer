# flask-react-prostate-cancer
Flask–React Prostate Cancer is a web-based application for prostate cancer risk prediction and analysis. The backend, built with Flask, exposes trained logistic regression models while the React frontend provides while the React frontend provides an interactive interface for clinicians  to input patient features, visualize predictions.
